import { SystemDefinition } from "../types/";
import { DataLoader, FakeInMemoryDataLoader } from "../dataLoader";
class BusinessEngine {
  constructor(definition: SystemDefinition, dataLoader: DataLoader) {}
}
